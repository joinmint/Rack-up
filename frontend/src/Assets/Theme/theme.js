import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#4EA872",
    },
    whitetext: {
      standard: "#FFFFFF",
    },
    secondary: {
      main: "#122C34",
    },
  },
});
